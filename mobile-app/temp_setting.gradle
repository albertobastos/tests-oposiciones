android {
            defaultConfig {
                applicationId = "info.albertobastos.mobile.TestsConstitucion"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }